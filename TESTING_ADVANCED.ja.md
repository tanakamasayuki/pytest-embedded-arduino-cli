# テストの応用

[English](TESTING_ADVANCED.md)

[テストの基本](TESTING_BASICS.ja.md) の続きです。実機テストで踏みやすい落とし穴と、標準機能で足りないときの手段を扱います。 何かが起きた後は、症状から引ける[よくある質問](TESTING_FAQ.ja.md)からどうぞ。

## pytest の基本の仕組み

このガイドで繰り返し出てくる 4 つの仕組みを、先にまとめます。

### テストの収集規則

pytest が自動で実行するのは、次の条件を満たすものだけです。

- ファイル名が `test_` で始まる `.py`
- その中の `test_` で始まる関数

**逆に言うと、`test_` を外せば自動では呼ばれません。** これは覚えておく価値があります。

いちばん多い用途は補助関数です。テストファイルに書くヘルパーは `test_` で始めてはいけません。始めてしまうと pytest がテストとして収集し、引数を fixture と解釈して失敗します。

```python
# ヘルパーは test_ で始めない
def wait_ready(dut):
    ...


def test_actual(dut):
    wait_ready(dut)
```

**テストファイルの名前は、プロジェクト全体で一意にしてください。** 同じ basename のファイルが 2 つあると、collection の時点でエラーになります。

```text
import file mismatch:
imported module 'test_plain' has this __file__ attribute:
  .../no_ino/test_plain.py
which is not the same as the test file we want to collect:
  .../with_ino/test_plain.py
```

`__init__.py` を置かないディレクトリでは、ファイルの basename がそのままモジュール名になるためです。sketch ディレクトリ名に合わせて `test_<sketch 名>.py` と付けておけば、自然に一意になります。

**同時に実行しないつもりのファイルでも一意にしてください。** 別々に動かしているうちは気づかず、あとで全体を流したときに初めて出ます。`--import-mode=importlib` を使えば重複を許せますが、名前を一意にするほうが単純です。

一時的にテストを止めたいときにも使えますが、その用途では勧めません。名前を変えると、止めていること自体が見えなくなります。理由が残る方法、つまり marker を使ってください。

### marker とは

marker はテストに付ける印です。`@pytest.mark.<名前>` の形でテスト関数に付けます。

pytest が意味を知っているものがいくつかあります。

| marker | 働き |
| --- | --- |
| `skip` | 常に飛ばす。理由を `reason` に書く |
| `skipif` | 条件が真のときだけ飛ばす |
| `xfail` | 失敗が既知。失敗しても全体は赤にならない |
| `parametrize` | 値を変えて同じテストを複数回実行する |

止めるならこう書きます。

```python
@pytest.mark.skip(reason="ハードウェア待ち")
def test_pending(dut):
    ...
```

これなら実行結果に skip として現れ、`reason` も表示されるので、止めていることと理由が残ります。名前から `test_` を外す方法との違いはここです。

**自分で名前を決めた marker も付けられます。** 意味は pytest が知らないので、`-m` での選別に使います。使う前に ini へ登録してください。登録しないと警告が出ます。`--strict-markers` を付けるとエラーになるので、綴り間違いを防げます。

```ini
[pytest]
markers =
    slow: 実行に時間がかかる
```

```bash
pytest -m "not slow"    # slow を除いて実行
pytest -m slow          # slow だけ実行
```

この形は、この後の「機材があっても既定から外したいとき」でも使います。

### conftest.py とは

`conftest.py` は、pytest が自動で読み込むファイルです。テストファイルと同じディレクトリか、その上位のディレクトリに置きます。import は不要で、置いてあれば読まれます。

**効く範囲は置いた場所の配下です。** プロジェクトの root に置けば全体に、sketch ディレクトリに置けばその sketch とその下だけに効きます。同じ名前の fixture が両方にあれば、テストに近いほうが勝ちます。

書けるものは 2 種類あり、**名前を誰が決めるか**が違います。

| 種類 | 名前 | 呼ばれ方 |
| --- | --- | --- |
| hook | pytest が決めている。増やせない | その名前に対応した時点で pytest が呼ぶ |
| fixture | 自分で決める | テストや他の fixture が引数に取ると呼ばれる。`autouse=True` なら要求されなくても呼ばれる |

**hook は定義済みの名前を実装するものです。** `pytest_runtest_setup` は各テストの直前、`pytest_sessionfinish` はセッションの終わりに呼ばれます。「テストの前」「セッションの終わり」のような、fixture では表せない位置に処理を差し込めます。使える名前の一覧は公式リファレンスの Hooks にあります。

<https://docs.pytest.org/en/stable/reference/reference.html#hooks>

綴りを間違えても気づけます。`pytest_` で始まる名前は hook として検査されるので、`pytest_runtest_setupp` のように書くと `unknown hook` エラーで実行が止まります。逆に `pytest_` で始まらない名前は、ただの関数として無視されます。

**hook の名前は pytest のものだけです。** plugin が hook の名前を増やすことはありません。`pytest-embedded` もこの plugin も増やしていません。plugin がしているのは、pytest の hook を実装することと、fixture と option を足すことです。したがって conftest に書ける hook を探すときは、pytest のリファレンスだけを見れば足ります。

| 層 | 足すもの |
| --- | --- |
| pytest | hook の名前、組み込み fixture、標準 option |
| `pytest-embedded` | fixture（`dut` など）、option（`--port`、`--baud`、`--root-logdir` など） |
| この plugin | fixture（`peers`、`arduino_test`、`arduino_cli_app` など）、option（`--profile`、`--run-mode`、`--device-lock` など） |
| プロジェクト | fixture、および pytest の hook の実装 |

**fixture は名前が自由なので、いくらでも増やせます。** いま使える fixture は次のコマンドで一覧できます。この plugin や `pytest-embedded` が提供する `dut`、`peers`、`arduino_test` などもここに出ます。

```bash
pytest --fixtures
```

書き方は公式リファレンスの Fixtures にあります。

<https://docs.pytest.org/en/stable/reference/fixtures.html>

**fixture はテストファイルにも書けますが、hook は書けません。** テストファイルに書いた hook は呼ばれません。つまり `conftest.py` が必須になるのは hook が必要なときだけで、fixture だけで足りるならテストファイルに置くほうが範囲が狭くて安全です。

強力ですが、使いどころは選んでください。理由と実際の用途は、後の「conftest.py は最後の手段」で扱います。

### 設定ファイル: pytest.ini と pyproject.toml

pytest の設定は 1 つのファイルから読まれます。候補は 4 つあり、上にあるものが勝ちます。

1. `pytest.ini` の `[pytest]`
2. `pyproject.toml` の `[tool.pytest.ini_options]`
3. `tox.ini` の `[pytest]`
4. `setup.cfg` の `[tool:pytest]`

`pytest.ini` は中身が空でも勝ちます。**2 つ置くと片方は完全に無視されます。** 設定を書いたのに効かないときは、まずどちらが読まれているかを疑ってください。実行時の出力に `configfile:` として表示されます。

書き方は形式によって違います。

```ini
# tests/pytest.ini
[pytest]
testpaths = unit single loopback peer
addopts = -m "not manual"
markers =
    manual: 人の操作を必要とする
```

```toml
# tests/pyproject.toml
[tool.pytest.ini_options]
testpaths = ["unit", "single", "loopback", "peer"]
addopts = "-m 'not manual'"
markers = [
    "manual: 人の操作を必要とする",
]
```

TOML では値に型があるので、複数行の設定は配列で書きます。`addopts` のように引用符を含む値は、外側と内側で別の引用符を使ってください。

よく使う項目です。

| 項目 | 働き |
| --- | --- |
| `testpaths` | 引数なしで実行したときの対象ディレクトリ |
| `addopts` | 毎回付ける option |
| `markers` | 自作 marker の登録 |
| `filterwarnings` | warning の扱い |

この plugin のリポジトリでは `pyproject.toml` に `testpaths = ["tests"]` を書いているため、引数なしの `pytest` は plugin 自身のテストだけを実行します。`examples/` を動かすときは対象を明示します。

環境によって有効無効が変わるテストの分け方は、次の節で扱います。

## 常設の機材と、そのときだけの機材

治具は 2 種類に分かれます。

- **常設の機材。** いつも繋がっていて、既定の実行で使えます。
- **臨時のもの。** 特定のテストのときだけ用意するものです。追加の board、センサ、アナライザ、給電を切れる装置のほか、**人の操作そのものも含まれます。** ボタンを押す、ケーブルを抜き差しする、磁石を近づける、といったテストは、人がその場にいる時間しか実行できません。

**分ける基準は台数ではなく、既定の実行でいつも揃うかどうかです。** 機材でも人でも同じ扱いです。常設が 3 台あるなら 3 台のテストを既定に入れてよく、常設が 1 台なら 2 台のテストが臨時扱いになります。

### peer の board は plugin が面倒を見ます

port か profile が解決できない peer があると、**その peer を使うテストは skip になります**。失敗しません。

```text
SKIPPED [1] peer device2: port is not resolved
```

つまり追加の board のために conftest を書く必要は、多くの場合ありません。繋いで port を設定した環境ではそのまま走り、繋いでいない環境では静かに skip されます。CI と実験机で同じテストファイルを共有できます。

**この skip は peer だけの挙動です。** primary DUT については skip されず、失敗として出ます。形は 3 通りあります。

**upload は接続より先に走ります。** そのため、指定した先に device が無い場合は接続時ではなく upload で落ちます。ログのどこを見るかが変わるので、順に並べます。

| primary の状況 | どこで落ちるか |
| --- | --- |
| port を一切指定していない | 接続の準備。`ValueError` |
| 指定した先に device が無い（存在しない path、切れた symlink を含む） | **upload**。`arduino-cli` が失敗し `CalledProcessError` になる |
| upload と接続のあいだに device が消えた | 接続時。`FileNotFoundError` |

`.env` に `/dev/serial/by-id/...` のような symlink を書いていると、**文字列としては解決できるのに実体が無い**という形になりやすいです。常設のはずのボードを外した机では、既定の実行がここで落ちます。primary はテスト対象そのものなので、無い状態は設定の誤りとして扱われます。skip にはなりません。

### plugin が知らない機材は自分で判定します

センサやアナライザ、給電を切れる装置のように、plugin が存在を知らない機材は、自分で有無を確かめて skip します。

```python
# manual/conftest.py
import os

import pytest


def pytest_runtest_setup(item):
    if not os.environ.get("TEST_POWER_SWITCH"):
        pytest.skip("給電を切れる装置が設定されていません")
```

conftest をディレクトリに置けば、その配下だけに効きます。

### 自動で判定できないものは既定から外す

**人の操作が必要なテストは、環境変数では判定できません。** その場に人がいるかどうかを plugin もテストも知りようがないからです。実行に非常に時間がかかるテストも、機材は揃っているが毎回は流したくない類です。こうしたものは既定の実行から外し、必要なときだけ指定します。方法は 3 つです。

**ディレクトリで分ける。** `testpaths` の対象から外し、必要なときだけ指定します。

```bash
pytest                 # testpaths のものだけ。manual は入らない
pytest manual/         # 必要なときだけ
```

**marker で分ける。** ini に登録し、既定では除外します。

```ini
[pytest]
markers =
    manual: いつも繋がっていない機材や人の操作を要求する
addopts = -m "not manual"
```

```bash
pytest -m manual       # marker を付けたものだけ
```

**ファイル名から `test_` を外す。** 収集規則を逆に使う方法です。ファイル名が `test_*.py` でなければ自動では収集されませんが、**ファイルを直接指定したときだけパターン照合が飛ばされ、収集されます**。

テストファイルは sketch ディレクトリの中に置く必要があるので、`manual/` の直下ではなく sketch ごとのディレクトリに入れます。

```text
  manual/
    manual_power_cycle/
      manual_power_cycle.ino
      sketch.yaml
      manual_power_cycle.py   <- test_ で始まらない
```

```python
# manual/manual_power_cycle/manual_power_cycle.py
def test_power_cycle(dut):     # 関数側の test_ は必要
    ...
```

```bash
pytest                                                  # 収集されない
pytest manual/                                          # 収集されない
pytest manual/manual_power_cycle/manual_power_cycle.py  # これだけ収集される
```

**ディレクトリ指定では収集されない点に注意してください。** ファイルを名前で指定する必要があります。まとめて動かすなら shell の展開を使います。

```bash
pytest manual/*/*.py
```

ini への登録も `testpaths` の調整も要らないので、**1 本ずつ動かすテストには手軽です。** 人が操作するテストはたいてい 1 本ずつ動かすので相性が良いです。代わりに、そのファイルにテストが入っていることが名前から分かりにくくなります。まとめて流すことが多いなら、ディレクトリか marker のほうが向いています。

どの方法でも構いません。要点は、**機材や人が揃わない環境で必ず失敗するテストを既定の実行に置かないこと**です。置くと失敗が常態になり、本当の失敗が埋もれます。

## 設定の優先順位と `.env`

port と define を環境ごとに差し替える仕組みです。[基本](TESTING_BASICS.ja.md) で触れた `.env` の全体像を並べます。

### primary DUT の port

上にあるものが勝ちます。

1. `--flash-port`
2. `--port`
3. `TEST_SERIAL_PORT_<PROFILE>`
4. `TEST_SERIAL_PORT`
5. `sketch.yaml` の `profiles.<profile>.port`。ただし `socket://...` の場合だけ

`<PROFILE>` は profile 名を大文字にし、`-` を `_` に置き換えたものです。

どれも解決できなければ **skip ではなく失敗** します。peer との違いはここです。

### peer DUT の port

peer は primary の指定を引き継ぎません。名前ごとに解決します。

1. `--peer-port <name>:<port>`
2. `TEST_SERIAL_PORT_PEER_<NAME>_<PROFILE>`
3. `TEST_SERIAL_PORT_PEER_<NAME>`
4. `sketch.yaml` の `socket://...`
5. 解決できなければ、その peer を使うテストは skip

`<NAME>` は `peer_` を除いたディレクトリ名を大文字にしたものです。`peer_echo` なら `ECHO` です。

### compile 時の define

`build_config.toml` の `[defines]` は、左が環境変数名、右が C/C++ 側の define 名です。

```toml
[defines]
TEST_WIFI_SSID = "WIFI_SSID"
TEST_WIFI_PASSWORD = "WIFI_PASSWORD"
```

```bash
# .env
TEST_WIFI_SSID=my-ssid
TEST_WIFI_PASSWORD=my-password
```

環境変数が未設定でも空文字が渡され、エラーにはなりません。値が必要なら、空文字を弾く判定を sketch 側かテスト側に書いてください。

### 使い分け

- **`.env`**: その machine の常設設定。git に入れません。
- **コマンドラインの option**: 一時的な上書き。`.env` より強いので、別の board を試すときに使います。
- **`sketch.yaml` の `socket://`**: host core のように board に依存しない接続先。リポジトリに入れて共有できます。

CI では `.env` を置かず、secret から環境変数として渡す形が扱いやすいです。host core だけを回すなら port の指定は不要で、`sketch.yaml` の `socket://localhost` がそのまま使われます。

## expect の落とし穴

### 末尾の可変長フィールドは行末で止める

シリアルの 1 行は分割して届きます。pattern の最後が可変長で、しかも改行で止めていないと、行の残りが届く前に match が確定し、切り詰めた値を取り出します。 **これはレースです。** 読み出しが行の残りの到着より先か後かを決めているものが無いので、同じテストが、ある実行では通り次の実行では落ち、しかも間違った値を持ったまま通ることもあります。この種類全体は *通ったり落ちたりするテスト* で扱います。

実際に `data=abcdef12` を出力した相手から `data=abcdef1` を読み取った例があります。

```python
# 悪い例。最後の capture が途中で確定しうる
dut.expect(re.compile(rb"data=([0-9a-f]+)"))

# 良い例。行末で止める
dut.expect(re.compile(rb"data=([0-9a-f]+)\r?\n"))
```

capture の後ろに同じ pattern 内の別のリテラルが続く場合は、そのリテラルが届くまで match しないので問題ありません。

**`[^\r\n]*` のように「行末で止める」つもりの書き方でも足りません。** ゼロ文字にも一致するので、行が届き切る前に match が確定します。行の完成を保証するには、`\r?\n` まで pattern に含めてください。ある実例では、監査したところこの形の pattern が複数のファイルにまたがって見つかり、**実際にテストを落としたものと、切り詰められうる値をそのまま使っていたものは別**でした。

### マッチした後の出力は保証されない

`dut.expect(...)` は pattern がマッチした時点で読み取りを終えます。その後にデバイスが送るバイトは `dut.log` に届く保証がなく、行の途中で切れることがあります。末尾まで確実に残したいときは、終端マーカーを出して `expect` するか、明示的にドレインします。

```python
import pexpect

dut.expect_exact("確認したい最後の行")
dut.expect(pexpect.TIMEOUT, timeout=2)   # 残りを読み切る
```

### 構造を変えた直後の失敗を、変更のせいと決めつけない

fixture の置き方や順序を変えた直後にテストが落ちると、変更が壊したと考えたくなります。しかし、**変更が変えたのは行の届くタイミングだけで、壊れていたのは元からあった読み取り側だった**、という例があります。device のログでは状態が正しく、テストが受け取った文字列だけが途中で切れていました。

**失敗を見たら、まず device 側のログで、被テスト側が本当に間違っているかを確かめてください。** タイミングに依存するバグは、構造を変えたときに初めて表に出ます。表に出したのは変更ですが、作ったのは変更ではありません。

## ログとアーティファクトの置き場所

2 種類あり、扱いが違います。

**シリアルログは `pytest-embedded` が管理します。** 実行ごとに UTC タイムスタンプ付きのディレクトリが作られ、その下にテストごとのディレクトリと `dut.log` が置かれます。**実行ごとに別のディレクトリなので、前回のログが上書きされることはありません。** 置き場所は `--root-logdir` で変えられます。この plugin は同じディレクトリに結果ファイル（`PASSED.txt` / `FAILED.txt` など、`SUMMARY.txt`、`summary.json`）も書きます。

つまり**実行後のログ退避は、多くの場合そもそも不要です**。CI で成果物として特定の場所へ集めたい、あるいは溜まり続けるのを整理したい、という要求があるときだけ考えてください。

**sketch 自身が書き出すファイルは別物です。** host core で動かす sketch が `fopen("output/...", ...)` のように書き出す場合、実行時のカレントディレクトリは sketch ディレクトリなので、ファイルは `tests/<name>/output/` に落ちます。ここは実行ごとに分かれないので、**前回のファイルが残ります**。残っていると、失敗した実行が成功に見えることがあります。これを消すのが、次の節でいちばん多く出てくる conftest の用途です。

## 中断時の後片付け

テスト本文の末尾に片付けを書くと、**途中で落ちたときに実行されません。** `assert` の失敗、`expect` の時間切れ、Ctrl-C のいずれでも同じです。

```python
# 悪い例。expect で落ちると stop に到達しない
def test_advertise(dut):
    dut.write("start\n")
    dut.expect_exact("ADVERTISING 1")
    dut.write("stop\n")            # 落ちたら実行されない
```

ボードは中途半端な状態のまま残ります。同じ module に次のテストがあればそれを巻き込み、なければ実行終了後もその状態が続きます。BLE のアドバタイズを出したまま終われば、隣の治具のスキャンに映り続けます。

**片付けは fixture の後処理へ移してください。** fixture の後処理はテストが失敗しても Ctrl-C でも実行されるので、どんな終わり方でも掃除されます。

### まずテストファイルに書く

掃除の内容はその sketch 固有なので、まずテストファイルに置くのが素直です。`conftest.py` は最後の手段です。

```python
import pytest


@pytest.fixture(autouse=True)
def cleanup_device(dut):
    yield
    dut.write("stop\n")


def test_advertise(dut):
    dut.write("start\n")
    dut.expect_exact("ADVERTISING 1")
```

`autouse=True` なので、そのファイルのテストすべてに効きます。効く範囲はその module だけです。

**名前は自由に付けられます。** ただし 2 つ守ってください。

- **掃除だと分かる名前にする。** `cleanup_device`、`stop_radio`、`release_pins` のように、何をするかが読み取れる名前にします。`autouse=True` の fixture はテスト本文に名前が出てこないので、名前が唯一の手がかりです。
- **`test_` で始めない。** `test_` で始めても fixture としては動きますが、**pytest はそれをテストとして収集せず、警告も出しません。** ファイルを眺めた人にはテストに見え、`@pytest.fixture` が何かの理由で外れた瞬間に、黙って本物のテストに変わります。

**掃除の時点でポートが開いていることは保証されます。** `dut` を要求しているので、この fixture は `dut` より後に setup され、`dut` より先に teardown されるからです。

peer も掃除するなら、`peers` を引数に取ります。

```python
@pytest.fixture(autouse=True)
def cleanup_device(dut, peers):
    yield
    for device in [dut, *[peers[name] for name in sorted(peers)]]:
        device.write("stop\n")
```

**応答を待つかどうかは任意です。** 上の例は送るだけで待っていません。待つ形にもできます。

```python
@pytest.fixture(autouse=True)
def cleanup_device(dut):
    yield
    dut.write("stop\n")
    try:
        dut.expect_exact("STOPPED", timeout=2)
    except Exception:
        pass          # 掃除の失敗でテスト結果を変えない
```

待つ利点は 2 つです。止まり切る前にポートが閉じて module の teardown で lock が解放され、別プロセスが upload を始める、という重なりを防げます。掃除の結果がログに残るのも利点です。

待たない利点も 2 つです。単純で、応答を返さない sketch でも書けます。sketch が固まっているときに待ち時間を払いません。

**待つ形にするなら例外を潰してください。** 掃除の失敗でテストの結果を変えないためです。応答が返らない状況は珍しくありません。sketch がコマンドを読む作りになっていない、固まっている、といった場合です。落ちたテストが teardown のエラーまで抱えると、本当の原因が見えにくくなります。

なお待たない場合、ポートを閉じた直後に sketch がコマンドを読み終えている保証はありません。確実にしたいなら待つ形にしてください。

### 複数の module で共有するなら conftest に移す

同じ掃除を多くの module に書くのが面倒なら、`conftest.py` に移します。効く範囲は置いた場所の配下です。中身はテストファイルに書くときと同じで構いません。

**置き場所には注意してください。** `dut` を要求する autouse fixture は、その配下の全テストにボードを要求します。`unit/` のようなボードを使わないテストを含むディレクトリの上には置けません。device テストだけが入っているディレクトリに置いてください。

```text
  tests/
    unit/                  <- ここには効かせない
    single/
      conftest.py          <- ここに置く
      my_app/
        test_my_app.py
```

`peers` も引数に取って構いません。`peer_*` ディレクトリを持たない module では空の mapping になるだけで、余分な処理は起きません。

### 同じ名前か別の名前か

`conftest.py` は上位のディレクトリのものも読まれます。上位と下位の両方に掃除の fixture があるとき、**名前を同じにするか変えるかで挙動が変わります。**

| 名前 | 挙動 |
| --- | --- |
| 同じ | 近いほうだけが動きます。上位のものは完全に隠れます |
| 違う | 両方動きます。近いほうが先に teardown され、次に上位、最後に `dut` が閉じます |

使い分けはこうです。上位の共通処理を、その sketch では別の内容に**差し替えたい**なら同じ名前にします。共通処理はそのまま動かして、その sketch でだけ**足したい**なら別の名前にします。

掃除を複数の fixture に分ける利点はないので、基本は同じ名前で 1 つにしてください。

### 何を止めるか

**まず「何を戻す必要があるか」を問うてください。それは module の構造で決まります。** 何かを止めることと、board を起動直後の状態へ戻すことは別の作業で、不具合が住んでいるのは後者です。

- **1 module 1 テストなら、止めるだけで足ります。** module 内で持ち越されるものは無く、次の module は upload から始まるので board がリセットされます。**次のテストのために状態を戻す必要が原理的にありません。** 残るのは、次に使う人のために治具を静かにしておくことだけで、それは止めれば済みます。しかも次の upload を待たずに静かになるので、共有の治具ではそのほうが親切です。
- **1 module に複数テストなら、戻す処理が必要になり**、そしてそれは間違えやすいものです。ある実例では、停止と「起動直後へ戻す」の両方を実装したところ、踏んだ不具合は全部が戻す側でした。登録数のカウンタだけ戻してリスナー本体を戻さない、外したコールバックを戻し忘れる、無線のフラグだけ戻す、特性値を戻し忘れる。**戻すとは「変えたものを全部数え上げる」ことで、失敗の形は「1 つ忘れる」です。** 止めるだけなら何も復元しないので、この族の不具合は発生しません。

**戻すときは、写しではなく実体を戻してください。** 先の不具合には共通の形があります。**状態は sketch の変数の中に無かった**、というものです。ライブラリの登録簿、コールバックの枠、サーバの属性、ハードウェアの中にありました。それを写しているだけの変数は単独で戻せてしまい、写しと実体がずれます。**これは戻さないより悪いです。** sketch が、自分が居ない状態を報告するようになるからです。写しに代入し直すのではなく登録処理をやり直し、**個数は自前で数えず実体から求めてください。** **そして、取り消せない操作もあります。** 無線のコントローラを次のリセットまで無効にしたのは設定ではなく事実で、それを記録したフラグだけ戻せば、既に消えたものを呼び続けるだけです。正直な復元は、その記録をそのまま残し、board は次の upload で戻ると割り切ることです。

つまり 1 module 1 テストという原則は、実行時間とは別のものも買っています。**書かずに済む復元コードです。**

何を止めるかはプロジェクト次第です。実績のある判断はこうです。

- **BLE のアドバタイズとスキャンは必ず止める。** 止めないと周囲の治具に干渉します。
- **PWM、サーボ、GPIO の駆動は止めたほうがよい。** 物理的に動き続けるものは危険です。
- **USB のようなペリフェラルは、止めなくても実害が小さいことがあります。** Arduino の USB スタックは一度 begin すると device の提示を止められません。止められないものは、止められる範囲で片付けて構いません。
- **応答を取るなら、sketch 側は「止まった」ときに出す。** 接続の全切断やアドバタイズの停止には時間がかかります。受信した直後に返すと、まだ止まっていない状態でテストが次へ進みます。応答を取らない構成でも構いません。

**後片付けはテスト開始時の正規化の代わりにはなりません。** 後片付けが守るのは環境です。テスト自身の正しさと、`-k` で 1 本だけ回せることは、開始時の正規化が担います。前のテストの後片付けが成功した前提でテストを書かないでください。

### リセットすればきれいになる、とは限らない

掃除の代わりにボードをリセットすれば元に戻る、と考えたくなります。**board によります。** 電源投入と同じところまで戻る board もあれば、一部が動いたまま残る board もあります。**確かめずに前提にしないでください。**

**ソフトウェアリセットがどこまで初期化するかは、board と core 次第です。** PWM の出力や GPIO の駆動が残るなら、リセットは掃除になりません。ピンの状態を保持する機能を有効にしている場合、それがリセットでは解けないこともあります。

例として ESP32 の場合を挙げます。Arduino の `ESP.restart()` は ESP-IDF の `esp_restart()` を呼び、その仕様はこうです。

> Peripherals (except for Wi-Fi, BT, UART0, SPI1, and legacy timers) are not reset.

つまりこの系では、PWM も GPIO も出力が続きます。ピン状態の保持を有効にしていれば、電源を落とすか解除の API を呼ぶまで解けません。**他の board では違う結果になります。** 使っている board で何が戻るのかは、core のドキュメントを見るか、実際に測って確かめてください。

**リセットすると起動処理が走り、また汚します。** これは board を問いません。`setup()` がもう一度実行されるので、止めたはずのアドバタイズや出力が作り直されます。掃除のつもりが元通りです。これを避けるには、「起動時に外へ影響を出さない」形にしておく必要があります。

**起動理由を見分けられる board なら、分岐できます。** 掃除のためのリセットと通常の起動を区別して、前者では何も始めない形です。ESP32 では `esp_reset_reason()` が使えます。

```cpp
// ESP32 の場合
void setup()
{
  Serial.begin(115200);

  if (esp_reset_reason() == ESP_RST_SW)
  {
    // 掃除のための再起動。外へ影響する処理は始めない
    return;
  }

  startEverything();
}
```

`loop()` 側でも始めないようにしてください。この API は電源投入、ソフトウェアリセット、パニック、ウォッチドッグなどを区別します。どの理由のときに待機するかは自分で決める必要があります。**同等の手段が無い board では、この分岐は書けません。** その場合はリセットに頼らず、止める API を呼ぶ形にしてください。

**peer をこの形にするときは注意が要ります。** 待機に入った peer は問い合わせにも答えないので、**症状が「ボードが死んでいる」と区別できません。** 待機中でも状態を答えるコマンドだけは生かしておくと、切り分けができます。

**リセットが戻すのは、リセット信号が届く範囲だけです。** サーボの角度、リレーの接点、ラッチする表示器はそのままです。I2C や SPI で繋がった device は自分の設定を保持します。無線の相手側も、こちらのリセットを知りません。

**同じボードに載っていても、リセット信号が届かないものがあります。** 無線のコントローラが別チップで動く構成、通信モジュールを UART で繋いだ構成、外付けのホストコントローラなどです。使う側は 1 枚のボードを 1 つの device だと思っているので、リセットすれば無線も止まると期待します。**止まりません。** メインの側が再起動するだけで、相手のチップはリンクやアドバタイズを保ったままです。

**不揮発領域は設計どおり残ります。** テストが書いた設定やログは、リセットでは消えません。消したいなら消す処理を呼ぶか、書き込み時に全消去する仕組みがあればそれを使ってください。**後者は platform ごとに指定の仕方が違い、そもそも用意されていない platform もあります。** 例として ESP32 では、profile の `fqbn` に付ける修飾子として用意されています。**どの platform でも取れるのは前者、つまり sketch 側で消すほうです。**

**リセットでポートが消えて再列挙される board があります。** native USB のボードが該当します。掃除のつもりのリセットが、次の実行の接続先を変えることがあります。

**初期化の方法は board と使っている機能ごとに違います。** 一律の手順はありません。止めたいものごとに、止める API を調べて呼んでください。**リセットは最後の手段です。** 止める API を呼ぶほうが速く、確実で、何が起きるかを読めます。

### 掃除コマンドが届かない場合の最後の手段

sketch が固まったり落ちたりしていると、掃除コマンドを送っても読まれません。**そして残留がいちばん酷いのは、まさにそういう実行です。** 治具を他のプロジェクトと共有していると、ボードが無線や USB を出したまま次の人に渡ります。

死んだ sketch にも効く手段は 1 つだけで、**session の終わりに無害な sketch を書き込むこと**です。書き込みはシリアルのやり取りに依存しないので、sketch が動いていなくても成立します。

実装は `conftest.py` の `pytest_sessionfinish` に置きます。この時点では fixture はすべて終わっているので、hook でしか書けません。

**device lock を自分で取ってください。** plugin の lock は module 単位で、module の teardown で解放されます。session の終わりにはもう保持していないので、そのまま書き込むと、待っていた別プロセスと衝突します。lock は次の名前で import できます。

```python
from pytest_embedded_arduino_cli.device_lock import DeviceLock, DeviceLockInfo, default_lock_dir
```

lock directory は `--device-lock-dir` の値か、指定がなければ `default_lock_dir()` です。key は symlink を解決した port path です。対象のボードは自分で把握してください。最後のテストがどのボードに繋いだかに頼ると、その実行で触っていないボードを取りこぼします。

この手段は時間がかかります。掃除コマンドで足りるなら、そちらを使ってください。

## conftest.py は最後の手段

`conftest.py` は pytest の拡張点です。テストファイルと同じディレクトリか、その上位のディレクトリに置くと効きます。上位に置けばプロジェクト全体、sketch ディレクトリに置けばその sketch だけ、と範囲を選べます。

**まず標準機能で足りないか確かめてください。** この plugin が用意しているものは次の通りで、多くの要求はこれで済みます。

- profile の選択（`--profile`、`sketch.yaml` の `default_profile`）
- port の解決（`--port`、`TEST_SERIAL_PORT_<PROFILE>`、`sketch.yaml` の `socket://`）
- 実行範囲の制御（`--run-mode=all|build|test`）
- compile-time define の注入（`build_config.toml`）
- peer DUT（`peer_<name>/` と `peers`）
- 物理デバイスの排他（`--device-lock`）
- 結果 summary（log directory への出力）
- デバイス側アサーション（Unity、ArduTest）

それでも conftest が必要になる場面はあります。以下は実際のプロジェクトで使われている形です。

### 1. sketch が書き出したファイルを実行前に消す

いちばん多い用途です。前回のファイルが残っていると、失敗した実行が成功に見えます。

```python
import shutil
from pathlib import Path


def pytest_runtest_setup(item):
    output_dir = Path(item.fspath).parent / "output"
    if output_dir.exists():
        shutil.rmtree(output_dir)
```

**fixture ではなく hook にする理由があります。** `pytest_runtest_setup` は `dut` fixture が sketch を build して起動する前に走ります。fixture で書くと、消す処理が sketch の書き込みと競合しかねません。順序を確実にするために hook を使います。

注意点として、この形は `output` という名前のディレクトリを無条件に削除します。他のリポジトリへ持っていくときは中身を確認してください。

### 2. compile より前に設定を書き換える

sketch.yaml の platform version を実行のあいだだけ差し替える、といった用途です。

```python
def pytest_collection_finish(session):
    # plugin が compile を始める前に書き換える
    ...


def pytest_sessionfinish(session, exitstatus):
    # 元に戻す
    ...
```

**`pytest_collection_finish` を使う理由**は、fixture の順序に関係なく compile より前だと保証できることです。書き換えと復元のあいだで異常終了すると作業ツリーが書き換わったまま残るので、実際の実装ではリポジトリ外にも元の内容を退避しています。

### 3. 複数の sketch で使う問い合わせヘルパーを共有する

[基本](TESTING_BASICS.ja.md) の方法 A を fixture にした形です。suite をまたいで使うので conftest に置きます。

```python
import pexpect
import pytest


@pytest.fixture
def probe():
    def ask(target, command, pattern, attempts=12, timeout=5):
        for _ in range(attempts):
            target.write(command)
            try:
                return target.expect(pattern, timeout=timeout)
            except pexpect.TIMEOUT:
                continue
        raise AssertionError(f"no answer to {command!r} matching {pattern!r}")

    return ask
```

### 4. シリアルログを検査して報告に足す

テストは通ったが、ログに気になる行が出ていた、という状況を拾う用途です。fixture でログを読み、`pytest_runtest_makereport` で報告に節を足し、`pytest_terminal_summary` で全体をまとめます。**報告に関わる hook が必要なので、conftest でしか書けません。**

このとき**その fixture に `dut` を要求させないでください**。要求すると `dut` より後に setup され、`dut` より先に後処理が走るため、ログの末尾を読めなくなります。`test_case_tempdir` だけを要求すれば正しい順序になります。

これは監査を足したことで生まれる制約ではなく、**もともと成り立っている条件**です。監査はログファイルを読むので、シリアルの listener が書き終えている必要があります。それが成り立つのは `dut` が先に finalize されるからです。つまり守るべき理由は、いま既に得られているものを壊さないことです。

### 5. 実行前に環境を用意する

ローカルの platform を Arduino のディレクトリへ symlink する、といった準備を session 単位で行う用途です。build より前に一度だけ実行する必要があるので、session scope の autouse fixture にします。

### 6. 実機を使わないユニットテストのヘルパー

`tests/` の下には、ボードを使わない純粋な Python のテストも置けます。`.ino` がないディレクトリに置けば、この plugin は compile も upload も行わず、普通の pytest として動きます。その fixture は通常の pytest の使い方で、plugin とは無関係です。

### conftest が不要なケース

- **1 つのテストファイルの中だけで使うヘルパー。** そのファイルに普通の関数として書けば足ります。
- **port や profile の切り替え。** option と環境変数で足ります。
- **テストごとの初期化。** テスト本文の先頭に書くほうが、読み手に見えます。

## ボードの状態を観測する

実行が終わった後にボードの状態を確かめたくなりますが、**同じボードに繋いで聞くと答えは信用できません。**

シリアルポートを開くと、pyserial が DTR と RTS を立てます。plugin がリセットのために操作しているのではなく、ポートを開く既定の動作です。この 2 線をリセット端子に直結している board では、これがそのままリセットになります。つまり問い合わせに答えるのは、起動し直した sketch です。停止したはずのアドバタイズを確かめたら起動時の状態が返り、失敗したように見えます。開く前に両線を落としても同じです。実際に peer は `ADVERTISING 1` と答え、Classic の DUT は起動時出力をそのまま流しました。

**リセットされるかは board によります。** 自動リセット回路を挟んでいる board では、開いただけではリセットされないことが多いです。native USB CDC の board も通常はリセットされませんが、1200 bps で開くと再起動します。**どちらであっても、対象の board に繋いで確かめる方法は信用できません。** リセットされる board では起動状態を読み、されない board でも接続そのものが sketch の動作に影響しうるからです。

**観測は別のボードから行ってください。** 対象には触れず、もう 1 台に観測用の sketch を焼いて聞きます。そして**対照も取ってください**。「聞こえない」だけなら、観測側が壊れていても同じ結果になります。対象をリセットして起動状態に戻し、同じ観測側で聞こえることを確認します。

### DTR と RTS を触らない option がない理由

plugin にはこの動作を変える option がありません。意図的にそうしています。

まず、**「触らない」という選択は存在しません。** ポートを開いた後、2 線には必ず何らかのレベルが乗ります。assert をやめても、解除側に落ちるかドライバが持っていた状態が残るだけです。古典的な ESP32 の回路では DTR が IO0、RTS が EN に繋がっているので、解除側で保持すると次のリセットで書き込みモードに入りかねません。つまり option は「触らない」ではなく「どちらのレベルで開くか」になり、正しい値は board ごとに違います。

次に、**テストがステートレスなら接続時のリセットは無害です。** 各テストが自分で状態を作るので、リセットされても困りません。困るのは module 内で状態を積み上げるテストですが、それは避けるべき書き方です。

必要になりうる条件は 1 つだけ考えられます。**接続でリセットされる board で、かつ起動に数秒かかる構成**です。module に複数テストがあると毎テスト起動待ちを払います。1 module 1 テストなら upload の時間が支配的なので、差はほぼ出ません。

どうしても必要なプロジェクトには逃げ道があります。`pytest-embedded` の `Serial` は、port 文字列の代わりに構築済みの pyserial オブジェクトを受け取れます。conftest で `serial` fixture を上書きし、`do_not_open=True` で用意して DTR と RTS を設定してから開く形が取れます。board 固有の判断になるので、plugin の option より conftest 向きの内容です。

## テスト計画の作り方

module に 2 本目のテストを足す費用は、それ自体は小さいです。upload は module に 1 回なので、2 本目以降が余分に払うのは接続のやり直しだけです。**ただしこれは board 次第で、接続のたびにリセットがかかる board では起動もやり直します。**重くなるのは、**テストごとの初期化と掃除**と、**順序依存**の 2 つです。前者は時間として、後者は複雑さとして効いてきます。

テスト計画が破綻するのは、テストが増えたときではなく、テストが互いに依存したときです。そして**順番を気にしなければならない時点で、そのテストの設計は間違っています。** 後に出てくる逆順の検査は、順序を管理するための道具ではありません。**設計の誤りを見つけるための道具です。** 見つかったら、順序を固定するのではなく設計を直してください。

### 計画の階層

計画は 1 回の実行ではありません。下ほど安くて頻繁、上ほど高くて稀な、いくつかの階層です。それぞれの層は、下の層には答えられない問いに答えます。

1. **host core で回すユニットテスト。** ハードウェアが要らないので、CI を含めてどこでも走り、同時に何本も走ります。ロジック、解析、状態遷移。**一番よく回す層です。** 手持ちで最も安く、境界条件の誤りをこれより早く教えてくれるものは上にありません。
2. **今触った機能の module テスト。** 実機を使いますが、目の前の変更に関係する module だけです。作業中に回し続けるのはこの層です。
3. **`--clean` を付けた、きれいな状態からの全実行。** 全部を、最初から、1 回の起動で流します。順序依存と状態の残りが出てくるのはここで、それは一部だけ流すと隠れるものです。マージ前やリリース前に回します。
4. **ビルドテスト。** 対応を謳っている profile すべてで example をコンパイルします。device も serial も無く、動作の良し悪しも見ません。**「まだビルドが通るか」だけです。**
5. **必要なら manual テスト。** 人が見る、聞く、抜き差しする必要があるもの。既定の実行からは外れていて、順番のどこに入るかは何を見るか次第です。

**普通の pytest も同居できます。** `dut` も `peers` も要求しないテストは、ただの pytest のテストで、他のテストと同じ 1 回の実行の中で一緒に走ります。データ表の確認、生成したヘッダの確認、ディスクリプタの中身の確認。**どれも core とは関係ないので、普通の Python として書いて、device の fixture に依存させなければそれで済みます。**

**1 つだけ条件があり、それは fixture ではありません。** compile と upload を起こすのは、**同じディレクトリに `.ino` があること**であって、テストが `dut` を要求したかどうかではありません。sketch の隣にハードウェア不要のテストを置くと、そのテストのためだけにビルドが走ります。**そうしたテストは `.ino` の無いディレクトリに置いてください。** そうすればプラグインは何もしません。

**host core の下に、もう 1 つ選択肢があります。** Arduino API に一切触れないコード、たとえば codec や parser や変換表なら、システムのコンパイラで直接ビルドして普通のプログラムとして走らせられます。テストからコンパイラを呼び、できたバイナリを実行して終了状態を見る形です。**ボードも core の導入も要らず、手に入る中で最も速い方法です。**

**引き換えに手放すのは、包んでもらうことです。** host core は OS 依存の部分を隠してくれますが、コンパイラの直叩きは隠してくれません。コンパイラの名前、フラグ、バイナリの置き場所、さらには `char` の符号の有無まで、自分で正しくする必要があります。`char` の符号は手元のマシンとターゲットで違うことがあり、しかも誰も検査してくれません。**結果として、別の OS や自分のものでない CI ランナーでは動かない可能性が出てきます。** テスト対象のコードが本当に OS 非依存であるときに使い、その周りの仕掛けは非依存ではない、と分かった上で選んでください。

**全実行に付ける `--clean` について。** これはコンパイルまで素通しするので、前回のビルド結果を何も再利用しません。加えてこのプラグインは、実行前に ArduTest の生成物も消します。**普段は付けなくて大丈夫なことが多いです。** 差分ビルドのほうがずっと速く、それで困らないからです。**付けるべきなのは、利用しているライブラリや core のバージョンを上げたときです。** 普段の速さを支えている「前回の再利用」が、そのときだけは邪魔になります。**リリース前は `--clean` を付けたフルテストを推奨します。**

**このプラグインが担当しないのは、ビルドテストの層だけです。** それ以外はすべてこのプラグインの上で走ります。manual テストも同じ仕組みで、既定の選択から外してあるだけです。ビルドテストには device も serial ポートも無く、要求する fixture もありません。**matrix で `arduino-cli compile` を回すだけの、別の形の作業です。** プラグインが将来面倒を見てくれるのを待つのではなく、別に用意してください。

**ビルドテストの問いは、下の層のどれも問いません。** どの実行も、1 つの sketch を 1 つの profile でビルドするだけだからです。手持ちの実機テストが全部通っても、一度も書き込まない profile が壊れていることはあります。

### ビルドテストは和ではなく積で増える

example 数 × profile 数です。example が 1 つ増えれば行が増え、profile が 1 つ増えれば列が増えます。そしてコンパイルは速くありません。この全部を毎回の push で回すと、push しなくなります。**2 つに分けてください。**

- **毎 push は狭く。** 重要な profile だけを、全 example に対して。ビルドを壊す変更を捕まえるにはこれで足ります。
- **全面的な掃き出しは必要なときだけ。** 全 profile、あるいは全 core version。リリース前に手で回すもので、毎 push で回すものではありません。

**掃き出しを現実的にするのは並行実行で**、ホスト型の CI ならそれが手に入ります。profile ごと、あるいは core version ごとに 1 ジョブを同時に走らせれば、実時間は合計ではなく**一番遅いジョブ**になります。GitHub Actions の matrix が代表的です。実際に効いてくるのは次の点です。

- **1 つ落ちても他のジョブを止めないこと。** ビルド matrix はカバレッジの報告なので、全部のセルが欲しいはずです。fail-fast は切ってください。
- **導入済みの platform をキャッシュする。** バージョンを固定している記述をキーにしておくと、core を上げていないジョブが再ダウンロードしなくて済みます。
- **カバレッジ matrix は、セルごとに成功・失敗・対象外を記録した上で、正常終了させる。** 赤いセルは情報です。マージを止めるゲートが要るなら、それは別のルールを持つ別のジョブです。
- **ディスクに気をつける。** platform の導入は大きくなることがあり、1 ジョブに複数入らない場合があります。**core version ごとにジョブを分ける**のが逃げ道です。
- **きれいに飛ばす。** ある profile を宣言していない example は、失敗ではなく対象外として報告されるべきです。

上記の形の実物のワークフローは、[実例プロジェクト](TESTING_EXAMPLES.ja.md)からリンクしています。

### うまくいかない 3 つの形

実際に見つかったのはこの 3 つです。どれも「前のテストがしたこと」に依存していて、どれも設計の誤りです。順序を決め打ちにして回避するのではなく、依存そのものを消してください。

**1. 前のテストが作った状態に相乗りする。** カウンタや接続数のように積み上がる値を、前のテストが作った前提で assert します。

- 症状: そのテストだけを実行すると落ちます。
- 直し方: 開始時に自分で状態を作ります。

**2. 最初に走ることに依存する。** 起動時に 1 度だけ出る行を待っています。前に別のテストが走ると、その行はもう流れ終わっています。

- 症状: 単体では通るのに、module 全体を流すと落ちます。順番を変えても落ちます。
- 直し方: 通知を待つのをやめ、状態を問い合わせます。

**3. 他のテストが汚す状態を、きれいな前提で assert する。** 片方は「何も接続していない」ことを前提にし、もう片方は接続を張ったまま終わる、という組み合わせです。

- 症状: 実行順によって結果が変わります。
- 直し方: 前提にする状態を、テストの開始時に自分で作り直します。作り直す手段が無いように見えたら、次の節を読んでください。

### 壊すテストは module ごと分ける

board を使えない状態にするテストがあります。周辺機能を無効化する、リセットしないと戻らない設定を書く、といったものです。こうしたテストは「最後に走らなければならない」ように見えます。

**これは複数テストを順番に並べる理由ではなく、module ごと分ける理由です。** 別の module にすれば次の upload が board を戻すので、**順序の制約そのものが消えます。** 同じ sketch ディレクトリにテストファイルを分けるだけで足ります。

長時間まわして資源の増減を測るようなテストも同じです。他と混ぜたくないなら、混ぜずに済む置き方をしてください。「このテストは最後」という決まりを作るより、決まりが要らない形にするほうが確実です。

### 検査は 2 つ

**逆順で 1 回流す。** これが日常の検査です。upload 1 回で済み、上の 3 形態をすべて捕まえます。**1 回の pytest 実行の中で、module のテストを逆順に並べて流します。** 1 本ずつ逆順に実行するのではありません。それをやると upload を本数だけ払うことになり、次に挙げるもう 1 つの検査と同じ費用になります。**とくに 2 番は単体実行では捕まりません。** 単体で回せば必ず最初になるからです。

**この検査の対象はテスト間で共有される状態なので、複数テストの module が残っている間の検査です。** 1 module 1 テストにすると反転する対象が無くなり、日常的に走らせる意味も無くなります。順序依存はまとめたテストの内側へ移りますが、それを内側でどう検査するかは別の話で、**後述**します。**複数テストの module がある間は回してください。** ある実例では、対象にした module の大半で欠陥が見つかり、しかもその過半は単独実行でも通常実行でも表に出ませんでした。**そして 1 module 1 テストへの移行を安全にするのもこの検査です。** まとめる前に、どの module が順序に寄りかかっていたかを教えてくれます。

```bash
pytest $(pytest my_app --collect-only -q | grep '::' | tac)
```

**このワンライナーは parametrize と相性がよくありません。** `test_payload[512]` のような nodeid がクォートされないので、shell が glob として解釈したり、パラメータに空白があると分割されたりします。確実なのは、反転だけを行う小さな plugin を `-p` で渡す形です。conftest を汚さずに済みます。

```python
# revorder.py
def pytest_collection_modifyitems(session, config, items):
    items.reverse()
```

```bash
PYTHONPATH=. pytest -p revorder my_app/
```

**結果は 3 値で読んでください。** `passed`、`failed`、そして `error` です。**`error` はテストの失敗ではなく環境の失敗**で、upload が落ちた、port が無い、といった意味です。2 値で数えると、環境の失敗を成功として集計します。実際に集計スクリプトがそれをやった例があります。

**多数が同時に落ちたら、テストではなく環境を疑ってください。** 連続して `error` が出るなら、そこで止めて配線と port を確かめるほうが速く済みます。

`conftest.py` に置いて切り替えられるようにしても構いません。

```python
def pytest_collection_modifyitems(items):
    items.reverse()
```

**1 本ずつ単体で流す。** `-k` や nodeid 指定で 1 本だけ回せることの基準です。テストの本数だけ upload を払うので、日常の検査より、規約を確かめたいときに使います。

```bash
pytest my_app/test_my_app.py::test_count
```

**どちらも、実際に走らせることが要点です。** 「このテストは前のテストの副作用で通っているのではないか」という疑いは、**grep では答えが出ません。** 実例では、問い合わせ型だと分類したテストが実は起動時の出力を読んでいたり、逆に接続の通知を待っていると思ったテストが実は最初に問い合わせていたりと、**読み違いは両方向に起きました。** 静的に数えて安心せず、順番を変えて走らせてください。

**とはいえ実行だけでも足りず、1 つの経路で全部は出ません。** 復元コードを見直した実例では、欠陥はそれぞれ違う経路で出てきて、**どの経路も 1 件ずつしか見つけませんでした。** 通常の全実行、逆順の実行、そして sketch の読み直しです。逆順が捕まえた 1 件は、単独実行でも通常実行でも出ませんでした。読み直しが捕まえたものは、どの実行でも発火しませんでした。それを暴くはずのテストが、たまたま module の後ろのほうに居たからです。**検査は走らせ、そのうえで復元の経路は読んでください。**

**機械的に探すのは失敗しました。** 「`setup()` で代入されない file scope の変数」という規則では、**コールバックの登録も、サーバ属性への値の書き込みも見えません。どちらも変数ではないからです。** 変数の形をした検索は、変数に入っていない状態を見つけられません。変数として見えていた分についても、規則の判断は逆でした。**`bool` の型にも名前にも、「戻してよい設定」と「取り消せないことが起きた記録」を区別する手がかりはありません。** 大量の候補を絞り込んでも残ったのは一握りで、本物はその中の 1 件でした。

### 通ったり落ちたりするテスト

**スイートが抱える不具合のなかで、これが最悪です。** 必ず落ちるテストは直されます。**ときどき落ちるテストは、もう 1 回実行されます。** そして再実行するようになったスイートは、もう信じられていないスイートです。この 1 本が、周りのテスト全部の価値を下げます。

**ここで言う「レース」は競合状態のことです。** 2 つのことのうち**どちらが先に起きるかで結果が変わり、しかもその順番を決めているものがどこにも無い**状態を指します。読み出しが行の到着より先か後か。テストの書き込みが peer の起動完了より先か後か。順番を保証するものが無いので、**同じテストを同じ入力で流しても、実行ごとに結果が変わりえます。** レースは「間違っているテスト」ではありません。**「答えが決まっていないテスト」です。** だから再実行すると直ったように見えます。

**まず 3 つの形を切り分けてください。原因も直し方も違います。** 何も変えずに、同じ選択で 2 回流すのが最初の一手です。

| 見えている症状 | 形 | 見る場所 |
| --- | --- | --- |
| 同じ選択の 2 回が食い違う | **非決定的。** どこかが時間に依存している | 次の段落と、その下の一覧 |
| 全実行では落ちるが、単独では毎回通る | **実行内の順序依存** | *うまくいかない 3 つの形* |
| いまは通るが、別の実行の後だと落ちる。単独でも逆順でも再現しない | **前回の実行が残した状態** | この節の後半 |

非決定的なのは 1 つ目だけです。**残りの 2 つは決定的**で、このガイドの別の場所に答えがあります。これらを「不安定」と呼ぶと、探す場所を間違えます。

**そして、非決定的だからレースとは限りません。** 自分の制御の外にあるものが、実行ごとに違う時間を要したときも 2 回は食い違います。アクセスポイントへの接続、DHCP、ネットワークのサービスの起動待ちなどです。**問うべきは「結果は決まっていて、ただ遅いだけなのか」「そもそも結果が決まっていないのか」です。** ばらつきはあっても最終的に接続できるものは前者で、**短すぎた timeout のほうが不具合**です。伸ばすのが正しい直し方で、塗り潰しではありません。行が完全に届いているか分からないまま読むのは後者で、**timeout をいくつにしても直りません。**

環境起因のほうは、確かに「数字を大きくする」がほとんどの答えです。そのうえで、これだけの差が出ます。

- **timeout は寛容に、ただし上限は持たせ、メッセージに相手の名前を入れる。** 長く待ったうえで「どのアクセスポイントを待っていたか」を言って落ちる実行は診断できます。ただ time out するだけの実行は診断できません。
- **接続が主題ではなく前提であるなら、テストの失敗ではなく環境のエラーとして報告する。** 前述の `error` と `failed` の区別です。**アクセスポイントが死んでいることを製品の失敗として数えるのが、集計が嘘になる経路です。**
- **retry するのは前提であって、アサーションではありません。** 前提を作る過程での上限付き retry は治具の頑丈さです。**被テスト対象を包む retry は不具合を隠します。** 線が引かれるのはここで、「retry という言葉」のところではありません。
- **テスト毎の経路から外に出す。** 毎テスト再接続していれば、ばらつきを毎テスト払います。
- **治具が自分のものなら、ばらつき自体を減らす。** オフィスの回線ではなくテスト専用のアクセスポイント、チャンネル固定、資格情報は `.env` に固定。**原因を許容するのではなく取り除く手段は、これだけです。**
- **経過時間で assert しない。** 接続時間そのものが主題である場合を除きます。

**レースになりやすいもの。**

- **行末まで待たない pattern。** シリアルの 1 行は分割して届きます。pattern の末尾が可変長で、改行で止めていないと、**届いた分で match が確定し、切り詰めた値を読みます。** 全体が取れるかどうかは**到着のタイミングだけ**で決まるので、同じテストが通ることも落ちることもあり、**最悪なのは間違った値を持ったまま通ることです。** `\r?\n` まで pattern に含めてください。`[^\r\n]*` では足りません。ゼロ文字にも一致するからです。詳しくは *末尾の可変長フィールドは行末で止める*。
- **1 度だけ通知されるものを待つ。** その行が、読み始める前に出てしまいうるなら、**そのテストは構造上レースです。** しかも `expect` は一致点より前を捨てます。**状態を問い合わせる形にしてください。** 次の節で扱います。
- **時間切れした `expect` の後に走るチェック。** 待っていた行は、遅れて届くことがあります。次のチェックがそれを拾い、無関係なところで落ちます。続けるのではなく、最初の失敗で止めてください。
- **観測できる条件ではなく、固定の待ち時間で assert する。** 空いているマシンでは通り、混んでいるマシンでは落ちます。
- **起動時の干渉。** peer を書き込んでいる間、primary は既に `setup()` を走らせているので、peer のリセットを観測しうります。**するかしないかは時間が決めます。** 直し方は *起動時に外へ影響を出さない* です。
- **受信をフィルタしていない。** 隣の機器のアドバタイズ、他人のリモコンから飛んできた赤外線、自分のテストが送っていない通信。これはテストを落とすことがあります。壊れたフレームとして届き、それをエラーとして報告する形です。**もっと悪いのは、通してしまう場合です。** 自分の peer が満たしていないアサーションを、周りの誰かが代わりに満たします。どちらにしても結果は**その時間帯に空中に何があったか**で決まるので、間違いではなく「ときどき」に見えます。
- **retry。** 上限のない retry は、はっきりした失敗を、ときどき落ちる失敗に変えます。**不具合を直すのではなく隠し、同時にスイートを信用できないものにします。**
- **独立に時間を持つ 2 つの側。** 無線の両端のように、どちらも相手の準備を待たない構成です。

**前回の実行が残した状態を運んでくるのは、peer が最も多いです。** primary は見ている対象で、しかも module の境界ごとに upload と起動が入るので、状態が意識に残ります。**peer の状態は意識に残りません。** そして peer の状態には、upload を越えて生き残るものがいくつもあります。

- **ペアリング、ボンディング、保存した設定は不揮発領域にあり**、upload が必ず消すとは限りません。primary と同じ board 依存です。
- **peer は、そもそも自分が書き込む board であるとは限りません。** 市販の device、計測器、他プロジェクトと共有している治具。**自分の実行の間に、誰もそれをリセットしません。**
- **無線の状態が、リセット信号の届かない別チップに載っていることがあります。** その場合、peer 自身が再起動してもリンクやアドバタイズは生き残ります。

結果として、**今日は自分のベンチで通り、明日は同じベンチで落ちる**テストになります。スイート側は何も変わっていません。**peer が絡むとき、単独実行で通ったことは独立性の証明になりません。**

**予防を、効く順に。**

- **前提は両側で、テストの中で作る。** primary の状態は作り直すのに peer は信用する、というのがよくある中途半端です。**peer にも同じ扱いが必要です。**
- **終わりだけでなく、開始時に正常化する。** 掃除は早期終了で飛びますが、**テストの開始は必ず走ります。** 実行を越えて残るものは、必ず消える場所で消してください。
- **観測できる条件で assert し、経過時間では assert しない。** timeout は寛容に。retry は前提を組み立てる間だけで、アサーションを包むのには使わない。
- **テスト専用の識別子を使う。** 隣のベンチが、自分の assert を代わりに満たしてしまわないようにします。受け入れは、受け取ってから捨てるのではなく**受信側で絞ってください。** **確かめ方は、peer の電源を切ってテストを流すことです。それでも通るなら、フィルタできていません。** 1 回の実行で済み、**「自分の device を検証しているテスト」と「部屋を検証しているテスト」を区別できるのはこれだけです。**
- **最初の失敗で止める。** 壊れた状態が、2 つ目の無関係な症状を作らないようにします。

**レースが直ったことは、1 回の緑では示せません。** 同じ選択を繰り返し流して、自分自身と一致することを要求してください。そして決定的にできないなら、**そのテストは製品側のレースを報告しているのかもしれません。** それは**厄介事ではなく発見です。** timeout を伸ばして塗り潰さないでください。**これは前述の環境起因とは正反対**で、あちらでは timeout を伸ばすことこそが正しい答えです。**報告の上では両者は見分けが付かず、扱いは逆になります。** だから 2 回流す切り分けを最初に置いています。

### 通知を待たず、状態を問い合わせる

3 形態のうち 2 番と 3 番は、同じ 1 つの変更で消えます。**起動時に 1 度だけ出る行を待つのをやめ、いつでも現在の値を答えるコマンドを sketch に用意します。**

```cpp
  else if (line == "state?")
  {
    Serial.print("STATE conn=");
    Serial.println(connected ? 1 : 0);
  }
```

テストはこれを問い合わせます。何番目に走っても答えは返ります。

**1 つの大きなテストにまとめるのは症状を消すだけで、問い合わせに変えるのは原因を消します。** まとめれば順序は固定されますが、依存そのものは残ります。問い合わせにすれば、どの順で走っても成立します。

### 停止と開始ができると、直せる範囲が広がる

「起動直後にしか成立しない状態」を assert するテストは、他のテストが先に走ると落ちます。設計の誤りですが、**直す手段が無ければ直せません。** 実際、「戻すコマンドが無いので順序をテストの中に閉じ込めるしかない」と判断して 2 本を統合した例があります。統合は順序を固定するだけで、依存は残ったままです。

device を止めて起こし直せるなら、その状態はいつでも作り直せます。テストの冒頭で止め、起こし直し、列挙や接続を待ってから assert します。これで順序への依存は消え、1 module 1 テストの形も保てます。

```text
stop → start → 準備できるまで待つ → assert
```

**この形は、停止がその状態を本当に消すことを前提にしています。** ライブラリによっては、停止してもキャッシュや登録が残ります。形だけ真似ても直りません。**停止が実際にその状態を消したことを、device のログで確かめてください。** 何が消えるかを仮定しないことです。

停止と開始のコマンドは、多くの場合「起動時に外へ影響を出さない」ために用意します。**書き込み窓を閉じるために入れた仕掛けが、副産物として順序依存を直す手段にもなります。** ゲートは、逆順の検査で誤りを見つけられるようにするだけでなく、見つけた誤りを直せるようにもします。

代償は作り直しの時間です。列挙や接続をやり直すぶんだけかかります。それでも、逆順の検査が永久に赤いままでいるよりは安いはずです。

**この費用は、窓を閉じるための停止と開始とは別物です。** 窓は module に 1 回しか開かないので、そちらは 1 回で済みます。一方こちらは、**pristine を必要とするテストの数だけ払います。** 混同しないでください。

**「戻す手段が無いから統合するしかない」と結論する前に、戻す手段を作れないかを確かめてください。** 統合はたいてい、その確認を飛ばした結果です。

### 粒度は device 側で稼ぐ

テストを分ければ失敗した箇所の名前が分かりますが、同じことを device 側でも得られます。sketch が複数の検査を実行し、結果と件数を出力し、pytest のテスト 1 本がそれを読む形です。upload も接続も 1 回で、失敗した検査の名前は sketch が出します。

```text
TEST_END pass=37 fail=0
```

**この形には限界があります。** 判定できるのは device の中にある情報だけです。**host 側にしかない証拠は判定できません。** 相手が descriptor から何を解釈したか、どのエンドポイントアドレスを割り当てたか、といったものです。そうした検証は host 側のテストに残してください。

なお、case ごとに device の姿を変えたいという理由だけで、この形を諦める必要はありません。device は再書き込みなしで再列挙できるので、同じ sketch の中で別の姿になり直せます。払うのは列挙の時間であって upload ではありません。

### 分けることで得られるものは、思ったより少ない

速くなるかどうかは、測るまで分かりません。固定費の割合が低い suite では、まとめても分けても実行時間がほとんど動かなかった実例があります。時間で決まらないなら利点で決めることになりますが、並べてみると、どれも別の手段のほうがうまく満たします。

- **失敗した場所が名前で分かる。** **これは手放さなくて済みます。** 各 case を名前付きの関数のまま残して順に呼べば、トレースバックが落ちたフレームにその case の名前を付けるので、まとめても「どの case で落ちたか」は分かります。case ごとの docstring もそのまま残ります。まとめて実際に失うのは、名前ではなく結果一覧の 1 行です。**検査ごとの記録が欲しいだけなら、device 側で報告するほうが確実です。**
- **1 本だけ実行できる。** 時間のかかる検証を切り出したいなら、**module ごと分けるほうが適切です。** 同じ sketch ディレクトリにテストファイルを 2 つ置けば module が分かれ、それぞれに upload が入って状態も独立します。ただし **module を分けるたびに compile と upload の両方がもう一度走ります。** compile は incremental build が効くぶん初回より速いことがありますが、無料ではありません。case ごとに module を作るのは高くつくので、切り出すのは重いものだけにしてください。
- **1 本落ちても残りは走る。** これは分割にしかない利点です。ただし実機では、失敗の後にボードの状態が崩れています。続きが当てにならないだけでなく、**中途半端な状態を次のテストが見て、無関係な失敗が並ぶことがあります。** 後片付けを入れるまで実際にそうなっていた、という例もあります。続けて走ることが害になる側です。

**逆順の検査が成立するのも、分割してからです。** これだけは分ける理由になりえます。次の節で扱います。

**そしてこの原則は、時間の測定では否定できないものを 1 つ買っています。書かずに済む復元コードです。** 1 module 1 テストなら module 内で持ち越されるものが無いので、掃除は止めるだけでよく、起動直後の状態へ戻す必要がありません。*何を止めるか* の節のとおり、**不具合が住んでいるのはその戻す側**です。上に挙げた、まとめても合計時間が動かなかった例でも、この理由は残りました。

つまり **1 つの module に複数のテストを置く積極的な理由は、ほとんどありません。原則は 1 module 1 テストとして扱ってください。** 分けたくなったら、まず module ごと分けられないか、device 側の報告で足りないかを考えます。それでも分けるなら、増える時間は後の節の式で決まります。

### 大きくなったテストは関数に分ける

**1 本のテストが大きくなってきたら、関数に分けてください。** テストを増やすのではなく、1 本のテストの中で名前付きの関数に分けます。1 つの関数が 1 つの機能ぶんの確認になるので、**テスト本体が「何を確かめているか」の一覧として読めるようになり**、残りを頭に置いたままにしなくても 1 か所だけ触れます。**見通しのための、普通の構造化です。** テストが自然に育った場合でも、module をまとめた結果その大きさになった場合でも、やることは同じです。

```python
def test_msc(dut):
    def capacity():
        dut.expect_exact("MSC_CAPACITY ok=1 blocks=16 block_size=512")

    def readback():
        dut.expect_exact("MSC_READ ok=1")

    for check in (capacity, readback):
        check()
```

**どちらの形でも構いません。** 入れ子の関数は `dut` を閉じ込めるので引数が要りません。module 直下に `_case(dut, peers)` という名前で置く形なら、引数は明示になり、ファイルは平らなままです。後者は**機械的に変換できます。** `def test_x(` を `def _x(` に置換して呼び出し側を生成するだけなので、既存の module をまとめるのに本文を書き直す必要がなく、case ごとの docstring もそのまま残ります。

**case は board を来たときの状態で返してください。** module の間はこれが保証されています。どの module も upload と起動から始まるからです。**case の間は誰も保証しません。** まとめるという行為は、module の内側の境界からその保証を取り除くことそのものなので、**upload が代わりにやってくれていたことを、各関数が自分で負う**ようになります。出てくる形は 2 つで、どちらもこのガイドに答えがあります。

- **board を使えない状態にする case。** リンクを切る、リセットするまで戻らない無効化をする、といったものです。**独立した module に出してください。** 壊すテストと同じ答えを、case の粒度で当てるだけです。
- **何かを意図的に切ったまま終わる case。** 「コールバックが無くても追従する」ことを示すために外す、といったものです。**その case 自身に戻させてください。** たいていは sketch にコマンドを 1 つ、case の末尾に 1 行足すだけで済みます。

**そのうえで、肝心の規則です。失敗は捕まえないでください。** 関数はリストから順に呼び、失敗はそのまま伝播させます。

**理由はトレースバックです。** 捕まえなければ、pytest は落ちたフレームにチェックの名前を付け、その行と、待っていた値をそのまま出します。

```text
test_usb_msc.py:61: in capacity
    dut.expect_exact("MSC_CAPACITY ok=1 blocks=16 block_size=512")
```

捕まえると、これが全部、自分で書いた 1 行の要約に置き換わります。**捕まえることは、証拠を捨てたうえで、その劣化版を手で作り直す作業を増やすだけです。**

シリアルにはもう 1 つ費用があります。**時間切れしたチェックは、待っていた行を読まずに残しています。** その行が遅れて届くと、次のチェックがそれを拾って、無関係なところで落ちます。続けるにはチェックの間でバッファを捨てる処理が要ります。**止めた場合より悪い結果に、複雑さを払ってたどり着く形です。**

これは前述の **1 本落ちても残りは走る** と同じ結論に、逆から到達したものです。実機では、1 つ落ちた時点で状態が信用できなくなっているので、その後に続くのは情報ではなく雑音です。

**リストから呼ぶのは飾りではありません。** まとめると順序依存がテストの内側に移るので、逆順の検査も内側に移す必要があります。リストは反転できますが、直接呼び出しの並びは反転できません。

**ただし、まとめたテストのすべてが反転の対象になるわけではありません。** 判定は「各関数が自分に必要な前提を自分で作っているか」です。作っているならリストは検査可能で、反転は本物の検査になります。**ある手順が直前の手順の後でしか意味を持たないなら、順序は偶然ではなく主題です。** 分かりやすいのはプロトコルのやりとりで、たとえば一群のフィールドを設定して読み戻し、次に 1 つだけを設定して、**残りが前回の要求の値を保っていること**を assert する形です。これを反転すると、誰も作っていない状態を assert することになります。**これは複数の case ではなく、1 つの case に複数の assert がある形です。** 見通しのために関数へ分けるのはよく、反転は付けないでください。反転しても意味の無いところにリストを差し出すと、fixture を足して落ちるのを見て「テストが壊れている」と結論する人が出ます。

**リストにすること自体が、走らせる前の検出器になります。** 呼び出しをリストに並べる作業は、「この case は何を残すか」を 1 件ずつ問う形になります。直接呼び出しを並べているあいだは、この問いが立ちません。ある実例では、この作業だけで、forward でも単独実行でも通っていた module から順序依存が 2 件出てきました。

```python
import os

import pytest


@pytest.fixture
def run_checks():
    def _run(checks):
        order = list(checks)
        if os.environ.get("REVERSE_CHECKS") == "1":
            order.reverse()
        for check in order:
            check()

    return _run
```

**比較は上のように値を厳密に見てください。** 「何かが入っていれば有効」と読む作りにすると、shell の profile に残った 1 行だけで、スイートが恒久的に逆順になります。**スイッチが無いより悪い状態です。** どの実行も監査になり、通常の検査が 1 回も走らなくなります。

**そしてスイッチ自体を検査してください。** 反転が反転しなくなると、監査は静かに 2 回目の順方向実行になります。これは後の節で、壊れると静かに緑になる他の検査と一緒に扱います。

### それでも分割が要る場合

絶対に必要な場面はほとんどありません。実際、複数テストの module を数えて調べた例では、**大半が「そう書かれたから」以上の理由を持っていませんでした。** それでも、まとめると戻せなくなるものが 2 つあります。どちらも「case ごとに何かを結びつけたい」という形です。

**まず、値を振る繰り返しはテストの中で書けます。** 送るペイロードの大きさを変えて試す、といった検証に、複数のテストは要りません。**そしてここだけは、失敗を集めて最後に assert する形が割に合います。** どの反復も同じ行を通るので、**どの値で落ちたかはトレースバックからは分からないから**です。集めることが、値を記録する唯一の手段になっています。名前の違うチェックを並べる場合はこの逆で、すぐ上の節のとおりです。

```python
def test_payload(dut):
    failures = []
    for size in (1, 64, 512):
        dut.write(f"send {size}\n")
        try:
            dut.expect_exact(f"SENT {size}", timeout=5)
        except Exception:
            failures.append(size)
    assert not failures, f"failed sizes: {failures}"
```

**失敗が device を使えない状態にするなら、途中で止めてください。** 集める形は、次の値にまだ意味があることを前提にしています。ある操作が落ちた結果、それ以降の値が信用できない状態になるなら、ループを抜けてください。ここで集めるのは便利だからであって、規則ではありません。

**1. case ごとに何かを紐づけたいとき。** ここが本当の分かれ目です。ループでは、値の 1 つだけに印を付けたり、値の 1 つだけを選んで回したりできません。

- **pytest の marker。** 既知の不具合を `xfail`、重いものを `slow`、環境依存を `skipif`。テストの一部には付けられません。
- **`-k` や nodeid での選択。** 特定の値だけを回して調べる、という操作はループではできません。
- **node id を鍵にするプロジェクト固有の仕組み。** シリアルログを検査する conftest が「この case でだけ出てよい行」の許可リストを持つ、といった形です。

3 つ目に実例があります。多数の操作を持つ module のうち 1 つだけが、仕様上あり得るエラー行を出します。case ごとに許可すれば、その 1 つだけを許せます。**まとめてしまうと許可の範囲を module 全体に広げるしかなく、本来許されない場面で同じ行が出ても気づけません。**

値を振る繰り返しにこの形が要るなら、`@pytest.mark.parametrize` を使います。**compile と upload は module に 1 回のままで**、値ごとに独立したテストになり、`test_payload[512]` のように名前が付き、値ごとに marker も付けられます。

**ただし parametrize も board を選びます。** 接続のたびにリセットがかかる board では、値の数だけ起動をやり直します。値ごとに状態を作り直す手間もそのぶん増えます。host core のように接続を閉じると終了する構成では、そもそも 2 件目が繋がりません。**値を振るだけならループで書けるので、board の性質を見てから選んでください。**

```python
import pytest


@pytest.mark.parametrize("size", [1, 64, 512])
def test_payload(dut, size):
    dut.write(f"send {size}\n")
    dut.expect_exact(f"SENT {size}")
```

ただし**細かければ良いというものではありません。** 前述の許可リストには、あえて広く書いた項目もありました。実行ごとに違う case へ現れる一過性の事象で、特定の case の性質ではなく実行の性質だからです。**鍵の細かさは、対象の実際の範囲に合わせてください。** なお、最初に一致した規則で止める実装にするなら、**細かい規則を広い規則より前に置いてください。** 順番を間違えると、広い規則が細かい規則を飲み込みます。

**2. 間違った理由で通っているアサーションを見つけたいとき。** これはもう 1 つと性質が違い、製品ではなくテスト自身の品質に関わります。

逆順の検査が意味を持つのは、**状態が共有されている場所だけ**です。そして状態が共有されるのは module の中だけです。他の 3 つの手段は、どれもこの検査を潰します。

- **1 本にまとめる。** 順序の仮定はテスト本文の中に移り、検査の対象から外れます。**ただし、検査も一緒に内側へ移せば別です。** 上のようにリストから呼ぶ形にして反転できるようにすれば、まとめても検査は生き残ります。自分で書いて保守する仕掛けなので、まとめない選択と比べてから決めてください。
- **module ごと分ける。** どの module も upload と起動から始まるので前提が常に成立し、検査が空振りします。
- **device 側で報告する。** 順序が firmware に固定されるので、並べ替えるには焼き直しが要ります。

そしてこの検査が見つけるのは、順序依存だけではありません。**テストが自分で作った前提ではなく、board がたまたま置かれていた状態によって満たされているアサーション**も見つかります。ある実例では、「探索の段階では device を使い始めない」ことを確かめるアサーションが、起動直後だからこそ成立していました。**これは空のアサーションではありません。** 探索が device を使い始めるようになる退行が起きれば、起動直後でも落ちます。ただし起動状態によって満たされていたので、**前に device を接続するテストを置くと偽陽性で落ちました。** 停止し、開始し、待ってから assert する形に直して、前に何が走っても成立するようになりました。

他の手段を採ると、この種のアサーションは静かに残り、しかも常に緑のままです。

**報告経路の故障に気づけないアサーションは別の欠陥クラスで、逆順の検査では見つかりません。** 前提を自分で作っても直りません。sketch が無条件に 0 を出力するようになった場合、「カウンタが 0 である」というアサーションは永久に緑のままです。**そのカウンタが動きうることを、テストの中で何も示していない**からです。

**ただし、候補の大半はこれではありません。** 「0 を主張していて、非ゼロを一度も観測していない」という条件でスイート全体を洗うと、リストは長く出ます。その大半は次の 3 つで説明でき、どれも欠陥ではありません。

- **主張ではなく、開始時の衛生確認である。** テストの開始時に読む 0 は、「前回の実行の残りが無い」ことを言っています。環境が満たしているのが正常で、**満たされないことこそが欲しかった合図**です。**製品について主張しているものだけが、製品の退行で落ちられる必要があります。**
- **同じテストの中の別のチェックが、値が動くことを示している。** 同じ case でなくても構いません。まとめるとは兄弟チェックを 1 つのテストに入れることなので、そのフィールドを非ゼロで見ている兄弟が 1 つあれば、動きうることの証拠になります。
- **1 行の中で値が冗長に符号化されている。** あるフィールドが隣のフィールドと同じ呼び出しから作られている場合、0 に固まった報告経路は**その相関ごと偽装しないと通りません。** 1 回の問い合わせから一緒に出しているフィールド群や、`submitted` / `completed` / `errors` のように誤り集計だけ死ぬと他の 2 つが合わなくなる組は、それだけで守られています。**書き換える前にこれを確かめてください。** 冗長に符号化された報告は、汚して戻すのと同じものを費用ゼロで買っています。

どれも当てはまらないときが、構造で直す場面です。**同じ case の中で実際に値を動かします。** 状態を汚し、変化したことを確認し、戻してから、きれいな値を assert します。こうすれば、報告経路が死んでいる場合は最後に通るのではなく確認の段階で落ちます。

**このクラスは稀だと思ってください。** この方法で監査した 2 つのスイートでは、順序依存が何件も出たのに対し、こちらは 1 件でした。**そして、この洗い出しは静的には終わりません。** ソースから `expect` のパターンを集める監査は、**変数で渡されたパターンを見られません。** ある実行では、それが偽陽性の最大の一群でした。静的な篩でリストを狭め、判断は読んで下すことになります。

**製品のアサーションを疑い終えたら、検査そのものを同じ目で見てください。** 検査もコードなので、この欠陥クラスが同じように当てはまります。`run_checks` の反転が反転しなくなれば、逆順監査は 2 回目の順方向実行になります。全 module が通り、監査は成功を報告し、何も検査されていません。**逆順実行が通ったことは、反転している証拠になりません。** 反転していなくても通るからです。単独実行の監査もシリアルログの監査も同じ形で、壊れれば静かに緑になります。

**見分け方は、その検査が失敗を報告した実例があるかです。** あるならその記録が証拠で、それ以上は要りません。無いなら、わざと壊して文句を言うことを確かめるか、**仕組みを直接アサートします。** 後者のほうが安いです。チェックのリストが頼んだ順で返ることを確かめるテストは device を一切必要としないので、`.ino` の無いディレクトリに置けば費用ゼロで回ります。順方向と逆方向の両方を assert し、あわせて**失敗がいまも伝播すること**も assert してください。最後の 1 つがあると、捕まえないという規則が、後の変更で静かに崩されないものになります。

まとめると、**表現力としては複数テストは要りません。検出力としては要ります。**

**経済性は理由になりません。** 「1 件ごとに upload するより、1 件ごとに作り直すほうが安い」は正しいのですが、それは複数 module と比べた話です。1 本にまとめても費用はほぼ同じなので、複数テストを選ぶ理由にはなりません。

**「この case だけ環境が違う」は、確かめてから言ってください。** ある実例では、profile を分ける必要があると思われていた 4 件が、依存していた機能のリリース後に確かめたところ全部同じ profile で通りました。この形の理由は、調べると消えることがあります。

### 重い準備はテスト毎の経路から外に出す

分割で増える時間は、次の形で決まります。

```text
増える時間 = (テスト数 - 1) × テスト毎の準備と後片付けにかかる実測時間
```

**2 つの計測を比べる前に、他に何が走っていたかを確かめてください。** 無関係なビルドがマシンを占有している間に取った値は、計測ではありません。見分け方は、**自分が触っていない module を見ること**です。それも同じくらい動いていたら、触った部分の数字が何を示していようと、その比較は無効です。ある例では「まとめたら遅くなった」と報告しかけましたが、コンパイル支配的な module が一様に伸びていて、しかも一切変更していない module も同じだけ伸びていました。

右側の 2 つ目を測らずに「分けるか、まとめるか」を議論しても答えは出ません。**「テストが多いから遅い」は誤診です。正しくは「テスト毎の準備が重いから遅い」です。** 同じ「テストを 1 本足す」という行為が、構成によって桁違いの費用になります。接続をやり直すだけの構成と、無線や USB を毎回止めて起こし直す構成では、比べものになりません。

**「その仕掛けが高い」と「その仕掛けを毎回払っているのが高い」は別物です。** 実例では、書き込み窓を閉じる仕掛けをテスト毎に払うと実行時間が大きく伸びましたが、module に 1 回へ寄せたところ、仕掛けが無いときとほとんど変わらない時間で同じ清浄さが得られました。**増えた時間のほとんどは、機能そのものではなく置き場所から来ていた**わけです。高いと感じたら、まず置き場所を疑ってください。

**重いと分かったときの直し方は、まとめることではなく外に出すことです。** まとめれば確かに速くなりますが、費用は module ごとに戻ってきます。module が 20 あれば 20 回払います。準備をテスト毎の経路から外に出せば、テスト数と無関係に効きます。**まず外に出すことを試し、それでも足りないときに統合を検討してください。**

**外に出すのは開始側だけではありません。** 開始を冪等にしても、**後片付けが毎テスト停止していれば、冪等の判定は一度も効きません。** 次のテストが始まる時点で停止済みなので、毎回まるごとやり直します。実例では、開始側を最初から冪等に書いていたのに費用が丸ごと残っていました。**開始を冪等にしたのに速くならないなら、停止が毎テスト走っていないかを見てください。**

開始側を外に出す方法は 2 つあります。

**sketch 側で冪等にする。** 開始コマンドを、2 回目以降は何もしない作りにします。Python 側に状態を持たなくて済むので、こちらが簡単です。

```cpp
  else if (line == "begin")
  {
    if (!started)
    {
      started = true;
      // 実際の開始処理
    }
    Serial.println("BEGUN");
  }
```

**Python 側で 1 回に絞る。** module 単位のフラグで守ります。

```python
@pytest.fixture(autouse=True)
def start_once(request, dut, peers):
    if not getattr(request.module, "_started", False):
        request.module._started = True
        dut.write("begin\n")
        dut.expect_exact("BEGUN")
    yield
```

**停止側は、いつ止めるかを決める必要があります。** 冪等にする対象がないので、開始側と同じ手は使えません。窓が module に 1 回しか開かないなら、停止も module の最後で 1 回にします。

```python
@pytest.fixture(autouse=True)
def usb_host(dut, peers, request):
    dut.write("begin\n")          # device 側で冪等。2 回目以降は何もしない
    yield
    items = [i for i in request.session.items if i.module is request.node.module]
    if not items or request.node is items[-1]:
        dut.write("stop\n")
        dut.expect_exact("STOPPED")
```

**fixture のスコープには制約があります。** `dut` と `peers` は function scope なので、**これらを要求する fixture を module scope にはできません。** 設定の時点で失敗します。上のようにフラグで絞るのは、この制約があるからです。

```text
ScopeMismatch: You tried to access the function scoped fixture peers
with a module scoped request object.
```

**順序にも落とし穴があります。** 何かを開始する autouse fixture は、**`peers` を引数に取らないと peer の upload より先に走ります。** 値を使わなくても、順序のためだけに取ってください。取り忘れると、peer を書き込んでいる最中に開始してしまい、それを避けるために入れたはずの仕掛けが効きません。ログには、開始した記録の直後に書き込み由来のエラーが並びます。

## peer テストの原則

2 台以上のテストで実績のある方針です。

- **テスト専用の識別子で相手を絞る。** BLE ならテスト専用の 128-bit Service UUID を使い、周囲の機器を除外します。device name だけで接続相手を決めないでください。
- **両側のシリアルログだけで合否を判定できる形にする。** 片側からの推測に頼らないと、どちらが悪いのか分かりません。
- **各テストの終了時に、スキャン、アドバタイズ、購読、接続を止める。**
- **無線環境による一時的な遅延には timeout を許すが、無制限の retry はしない。** 無制限に retry すると不具合を隠します。
- **切断理由、MTU、セキュリティ状態は、可能な範囲で両側で照合する。**
- **可能なら一方を標準ライブラリの直接実装にする。** 自作ライブラリ同士では相互接続性が確かめられません。
- **状態を持つテストは、開始時と終了時の状態を明示する。** ペアリング情報や不揮発領域のように、実行をまたいで残るものは特にそうです。

## 起動時に外へ影響を出さない

peer を使う構成では、書き込みの順序に注意が必要です。primary の upload は module 単位で先に走り、peer の upload は最初のテストの setup 中に走ります。つまり **primary が起動して `setup()` を実行した後に、peer が書き込まれます**。

primary が `setup()` で無線や USB を有効にしていると、peer の書き込み中のリセットや列挙を観測してしまい、エラーとして記録されることがあります。sketch の不具合ではありません。

対策は、**`setup()` では外へ影響する動作を始めず、テストから指示されてから始めること**です。テスト本文の 1 行目で有効化すれば、peer の書き込みは既に終わっています。

この形にすると、次の 2 つが問題になります。どちらも実機で起きます。

### 有効化の応答は起動時出力を飲み込む

有効化コマンドを送って `expect` で応答を待つと、**応答より前に出ていた行は読み取り位置の後ろに消えます。** `expect` はマッチした位置まで読み進め、その手前は捨てるからです。sketch が起動の途中で出した接続行や列挙のダンプは、そのあとのテストから見えません。sketch が出していないように見えるので、原因の分かりにくい失敗になります。

| sketch の出し方 | その行を `expect` できるか |
| --- | --- |
| 応答より前に出す | できない。timeout で失敗する |
| 応答より後に出す | できる |

つまり **起動時の出力は応答より後に出してください。** 順序を変えるだけで、sketch の処理内容は変えなくて済みます。

### いつ応答するかは選択が必要

応答を出すタイミングは、どちらに寄せても失敗の形があります。

- **周辺機器を起動した直後に応答する。** テスト本文が、列挙も接続も終わっていない状態で走り始めます。相手に書いても届きません。
- **実際に使える状態になってから応答する。** 上の節のとおり、起動時の出力が応答より前に出て飲み込まれます。

**両方を満たす形は 1 つです。** 使える状態になるまで応答を遅らせ、**応答したあとで起動時の行を出し直します。** データを扱うテストは device が生きていることを要求し、ライフサイクルを見るテストは接続行が見えることを要求します。1 つの sketch が両方のテストを持つなら、この形になります。

ここまでの内容を実際に使っているプロジェクトは [実例集](TESTING_EXAMPLES.ja.md) にまとめてあります。
